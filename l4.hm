(class Coordinate
        (method init (self x y z)
        (update self.x x)
    (update self.y y)
    (update self.z z)))

(class Object
        (method start (self)
        (define a (! Coordinate.new 2 3 4))
    a.x))

